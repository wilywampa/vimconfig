diff --git a/src/eval.c b/src/eval.c
--- a/src/eval.c
+++ b/src/eval.c
@@ -12335,6 +12335,7 @@
     int		n = FALSE;
     static char	*(has_list[]) =
     {
+	"sys_echo_off",
 #ifdef AMIGA
 	"amiga",
 # ifdef FEAT_ARP
@@ -18603,7 +18604,7 @@
 	int		i;
 
 	res = get_cmd_output(get_tv_string(&argvars[0]), infile,
-					   SHELL_SILENT | SHELL_COOKED, &len);
+					   SHELL_SILENT | SHELL_COOKED | SHELL_SLEEP, &len);
 	if (res == NULL)
 	    goto errret;
 
@@ -18645,7 +18646,7 @@
     else
     {
 	res = get_cmd_output(get_tv_string(&argvars[0]), infile,
-					   SHELL_SILENT | SHELL_COOKED, NULL);
+					   SHELL_SILENT | SHELL_COOKED | SHELL_SLEEP, NULL);
 #ifdef USE_CR
 	/* translate <CR> into <NL> */
 	if (res != NULL)
diff --git a/src/os_unix.c b/src/os_unix.c
--- a/src/os_unix.c
+++ b/src/os_unix.c
@@ -4150,7 +4150,9 @@
 	goto error;
 
     out_flush();
-    if (options & SHELL_COOKED)
+    if (options & SHELL_SLEEP)
+	settmode(TMODE_SLEEP);		/* disable echo */
+    else if (options & SHELL_COOKED)
 	settmode(TMODE_COOK);		/* set to normal mode */
 
     /*
diff --git a/src/version.c b/src/version.c
--- a/src/version.c
+++ b/src/version.c
@@ -60,6 +60,8 @@
 
 static char *(features[]) =
 {
+	"+sys_echo_off",
+	"+mac_clipboard",
 #ifdef HAVE_ACL
 	"+acl",
 #else
diff --git a/src/vim.h b/src/vim.h
--- a/src/vim.h
+++ b/src/vim.h
@@ -968,6 +968,7 @@
 #define SHELL_SILENT	16	/* don't print error returned by command */
 #define SHELL_READ	32	/* read lines and insert into buffer */
 #define SHELL_WRITE	64	/* write lines from buffer */
+#define SHELL_SLEEP	128	/* disable echo */
 
 /* Values returned by mch_nodetype() */
 #define NODE_NORMAL	0	/* file or directory, check with mch_isdir()*/
